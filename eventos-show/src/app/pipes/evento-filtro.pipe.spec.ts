import { EventoFiltroPipe } from './evento-filtro.pipe';

describe('EventoFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new EventoFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
